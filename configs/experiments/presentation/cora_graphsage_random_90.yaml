base: base/default
dataset: cora
model: graphsage
preset: fast_debug

run:
  experiment_name: presentation_cora_graphsage_random_90
  output_dir: runs/presentation/cora_graphsage_random_90

pruning:
  method: random
  target_sparsity: 0.9
  structured: true
  finetune_epochs: 10

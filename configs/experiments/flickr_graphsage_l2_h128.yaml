base: base/default
dataset: flickr
model: graphsage
preset: fast_debug

run:
  experiment_name: flickr_graphsage_l2_h128
  output_dir: runs/flickr_graphsage_sweep/l2_h128

model:
  name: graphsage
  num_layers: 2
  hidden_channels: 128

pruning:
  methods: [random, global_magnitude]
  sparsity_levels: [0.5, 0.9]
  structured: true
  finetune_epochs: 5

base: base/default
dataset: pubmed
model: gcn
preset: fast_debug

run:
  experiment_name: presentation_pubmed
  output_dir: runs/presentation/pubmed

pruning:
  method: global_magnitude
  target_sparsity: 0.9
  structured: true
  finetune_epochs: 5

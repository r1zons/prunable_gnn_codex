training:
  epochs: 500
  lr: 0.005
  weight_decay: 0.001
  early_stopping_patience: 100
run:
  experiment_name: robust

dataset_overrides:
  reddit:
    training:
      epochs: 300
      early_stopping_patience: 60
